<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">

    <div class="container mx-auto p-10">
        <a href="/" class="text-blue-500">Back to home</a>
        <h1 class="text-4xl font-bold tracking-tighter mb-8 text-center">All Users</h1>

        <% if (users.length === 0) { %>
            <p class="text-center text-gray-400">No users yet.</p>
        <% } else { %>
            <div class="users flex gap-6 flex-wrap justify-center">
                <% users.forEach((user) => { %>
                    <div class="user w-72 p-6 bg-zinc-800 rounded-2xl shadow-lg">
                        <div class="w-full h-52 rounded-lg overflow-hidden">
                            <img src="<%= user.image %>" alt="<%= user.name %>" class="w-full h-full object-cover">
                        </div>
                        <h3 class="text-2xl font-semibold mt-4"><%= user.name %></h3>
                        <h5 class="text-sm text-gray-400"><%= user.email %></h5>
                        <div class="flex gap-3 mt-4">
                            <a class="text-blue-500" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>

</body>
</html>
