<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center p-4">
    <h1 class="text-3xl font-bold mb-6">Todo Tasks</h1>
    <div class="w-full max-w-md">
        <form action="" class="flex flex-col bg-white p-6 rounded-lg shadow-md">
            <input type="text" placeholder="Title" class="title mb-3 p-2 border border-gray-300 rounded" name="title">
            <textarea placeholder="Description" class="description mb-3 p-2 border border-gray-300 rounded h-24" name="detail"></textarea>
            <input type="submit" value="Create Task" class="bg-blue-500 text-white p-2 rounded cursor-pointer hover:bg-blue-600">
        </form>
    </div>
    <!-- <div class="tasks w-full max-w-md mt-6"> -->
        <!-- <% if(files.length > 0) { %>
            <% files.forEach(function(val){ %> -->
                <!-- <div class="task1 hidden bg-white p-4 rounded-lg shadow-md mb-4 flex flex-col items-center">
                    <div class="mb-2">Task 1</div>
                    <div class="mb-2">Task 1 is nothing but sleeping</div>
                    <a href="class = 'text-blue">Read More</a>
                    </div> -->
            <!-- <% }) %>
        <% } else { %>
            <h3 class="text-zinc-600">No Tasks yet</h3>
        <% } %> -->
    <!-- </div> -->
    <script src="/javascripts/script.js"></script>
</body>
</html>
